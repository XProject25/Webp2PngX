# Webp2PngX
`webp2pngx.sh` is a safe, fast, and flexible Bash tool for converting `.webp` images into `.png` (or other formats supported by `dwebp`).   It recursively scans a directory, converts each `.webp`, preserves folder structure, and optionally removes or archives the original files.
